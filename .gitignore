# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# logs / deps (if a build step is added later)
node_modules/
*.log

# local env / secrets — never commit
.env
.env.*
*.pem
.dev.vars
.dev.vars.*

# Cloudflare Pages / Wrangler local state
.wrangler/
